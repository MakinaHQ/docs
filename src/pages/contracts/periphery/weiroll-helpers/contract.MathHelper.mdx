---
title: "MathHelper"
---

# MathHelper

[Git Source](https://github.com/MakinaHQ/makina-periphery/blob/72aab70ea8dcedba4658cd339dd07022e28ca1b2/src/weiroll-helpers/MathHelper.sol)

## Functions

<a id="add-uint256-uint256"></a>

### add

Returns the addition of two uint256.

```solidity
function add(uint256 a, uint256 b) external pure returns (uint256);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="sub-uint256-uint256"></a>

### sub

Returns the subtraction of two uint256.

```solidity
function sub(uint256 a, uint256 b) external pure returns (uint256);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="mul-uint256-uint256"></a>

### mul

Returns the multiplication of two uint256.

```solidity
function mul(uint256 a, uint256 b) external pure returns (uint256);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="div-uint256-uint256"></a>

### div

Returns the division of two uint256.

```solidity
function div(uint256 a, uint256 b) external pure returns (uint256);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="ceildiv-uint256-uint256"></a>

### ceilDiv

See `Math`.

```solidity
function ceilDiv(uint256 a, uint256 b) external pure returns (uint256);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="muldiv-uint256-uint256-uint256"></a>

### mulDiv

See `Math`.

```solidity
function mulDiv(uint256 x, uint256 y, uint256 denominator) external pure returns (uint256);
```

**Parameters**

| Name        | Type      | Description |
| ----------- | --------- | ----------- |
| x           | `uint256` |             |
| y           | `uint256` |             |
| denominator | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="ceilmuldiv-uint256-uint256-uint256"></a>

### ceilMulDiv

See `Math`.

```solidity
function ceilMulDiv(uint256 x, uint256 y, uint256 denominator) external pure returns (uint256);
```

**Parameters**

| Name        | Type      | Description |
| ----------- | --------- | ----------- |
| x           | `uint256` |             |
| y           | `uint256` |             |
| denominator | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="sqrt-uint256"></a>

### sqrt

See `Math`.

```solidity
function sqrt(uint256 a) external pure returns (uint256);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="average-uint256-uint256"></a>

### average

See `Math`.

```solidity
function average(uint256 a, uint256 b) external pure returns (uint256);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="ternary-bool-uint256-uint256"></a>

### ternary

See `Math`.

```solidity
function ternary(bool condition, uint256 a, uint256 b) external pure returns (uint256);
```

**Parameters**

| Name      | Type      | Description |
| --------- | --------- | ----------- |
| condition | `bool`    |             |
| a         | `uint256` |             |
| b         | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="max-uint256-uint256"></a>

### max

See `Math`.

```solidity
function max(uint256 a, uint256 b) external pure returns (uint256);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="min-uint256-uint256"></a>

### min

See `Math`.

```solidity
function min(uint256 a, uint256 b) external pure returns (uint256);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="log2-uint256"></a>

### log2

See `Math`.

```solidity
function log2(uint256 x) external pure returns (uint256 r);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| x    | `uint256` |             |

**Returns**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| r    | `uint256` |             |

<a id="log10-uint256"></a>

### log10

See `Math`.

```solidity
function log10(uint256 x) external pure returns (uint256 r);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| x    | `uint256` |             |

**Returns**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| r    | `uint256` |             |

<a id="log256-uint256"></a>

### log256

See `Math`.

```solidity
function log256(uint256 x) external pure returns (uint256 r);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| x    | `uint256` |             |

**Returns**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| r    | `uint256` |             |

<a id="eq-uint256-uint256"></a>

### eq

Returns whether two values are equal.

```solidity
function eq(uint256 a, uint256 b) external pure returns (bool);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type   | Description |
| ------------ | ------ | ----------- |
| &lt;none&gt; | `bool` |             |

<a id="lt-uint256-uint256"></a>

### lt

Returns whether a value is less than another value.

```solidity
function lt(uint256 a, uint256 b) external pure returns (bool);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type   | Description |
| ------------ | ------ | ----------- |
| &lt;none&gt; | `bool` |             |

<a id="lte-uint256-uint256"></a>

### lte

Returns whether a value is less than or equal to another value.

```solidity
function lte(uint256 a, uint256 b) external pure returns (bool);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type   | Description |
| ------------ | ------ | ----------- |
| &lt;none&gt; | `bool` |             |

<a id="gt-uint256-uint256"></a>

### gt

Returns whether a value is greater than another value.

```solidity
function gt(uint256 a, uint256 b) external pure returns (bool);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type   | Description |
| ------------ | ------ | ----------- |
| &lt;none&gt; | `bool` |             |

<a id="gte-uint256-uint256"></a>

### gte

Returns whether a value is greater than or equal to another value.

```solidity
function gte(uint256 a, uint256 b) external pure returns (bool);
```

**Parameters**

| Name | Type      | Description |
| ---- | --------- | ----------- |
| a    | `uint256` |             |
| b    | `uint256` |             |

**Returns**

| Name         | Type   | Description |
| ------------ | ------ | ----------- |
| &lt;none&gt; | `bool` |             |

<a id="uint128max"></a>

### uint128Max

Returns the maximum value of uint128.

```solidity
function uint128Max() external pure returns (uint128);
```

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint128` |             |

<a id="uint256max"></a>

### uint256Max

Returns the maximum value of uint256.

```solidity
function uint256Max() external pure returns (uint256);
```

**Returns**

| Name         | Type      | Description |
| ------------ | --------- | ----------- |
| &lt;none&gt; | `uint256` |             |

<a id="scaleamount-uint256-uint8-uint8"></a>

### scaleAmount

Scales `amount` from `fromDecimals` to `toDecimals`.

<i>

Converts token amounts between different decimal precisions.

</i>

**Note:**

- **warning:** Scaling down reduces precision due to integer division truncation.

```solidity
function scaleAmount(uint256 amount, uint8 fromDecimals, uint8 toDecimals) public pure returns (uint256 scaled);
```

**Parameters**

| Name         | Type      | Description                   |
| ------------ | --------- | ----------------------------- |
| amount       | `uint256` | The amount to scale.          |
| fromDecimals | `uint8`   | The source decimal precision. |
| toDecimals   | `uint8`   | The target decimal precision. |

**Returns**

| Name   | Type      | Description                                 |
| ------ | --------- | ------------------------------------------- |
| scaled | `uint256` | The amount adjusted to the target decimals. |

## Errors

### DecimalsOutOfRange

```solidity
error DecimalsOutOfRange();
```
